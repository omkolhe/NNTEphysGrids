function [xsorted,indexsort] = peakSort2DArray(x,direction,dimension)

xpeak = mean(x,dimension);

if dimension == 2
    xsorted = x(indexsort,:);
end
if dimension == 1
    xsorted = x(:,indexsort);
end

