function [PA] = getPhaseAllignment(xgp,parameters)
% Ref: Spontaneous travelling cortical waves gate perception in behaving
% primates, Nature 2020

PA = zeros(parameters.rows,parameters.cols,size(xgp{1,1},2));

N = size(xgp,2);
x

for t=1:size(time,2)
    for i=1:parameters.rows
        for j=1:parameters.cols
            PA =    
        end
    end
end
